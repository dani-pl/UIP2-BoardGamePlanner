import { View, Text, SafeAreaView } from 'react-native'
import React from 'react'

export const WalkthroughStart = ({next}) => {
  return (
        <SafeAreaView>
            <View>
                <Text> Walkthrough start</Text>
            </View>
        </SafeAreaView>
  )
}